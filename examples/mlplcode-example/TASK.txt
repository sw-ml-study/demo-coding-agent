First READ examples/tiny-mlpl-project/lib.mlpl and examples/tiny-mlpl-project/tests/test_add.mlpl and copy their syntax exactly: every function is written def u:name(args) { "docstring"; body }. Then create a new MLPL module work/mlplcode-example/hello.mlpl that starts with a comment line (MLPL comments begin with #, there is no //) and defines u:hello(name) returning the string "hello, " followed by name (use str_concat). Then create work/mlplcode-example/tests/test_hello.mlpl that starts with a # comment line, has the two lines include "../vendor/mlplunit.mlpl"; and include "../hello.mlpl";, one @test {name: "hello greets by name", tags: ["hello"]} function u:test_hello with a docstring that asserts u:assert_eq(u:hello("world"), "hello, world", "greeting"), and ends with u:run_registered_tests(). Then run: RUN mlpl work/mlplcode-example/tests/test_hello.mlpl. Reply DONE only after the run observation shows status: ok with the test passed.
