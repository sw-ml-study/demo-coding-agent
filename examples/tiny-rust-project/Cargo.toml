[package]
name = "tiny-rust-project"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Fixture crate the coding agent edits under test."

[dependencies]

# Stand-alone: keep this fixture crate out of the repository workspace so
# `cargo test --manifest-path examples/tiny-rust-project/Cargo.toml` works.
[workspace]
