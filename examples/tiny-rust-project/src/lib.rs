//! Fixture crate for demo-coding-agent. The agent's first task is to add a
//! unit test for `add` and make it pass. Keep this file tiny on purpose.

/// Add two integers.
pub fn add(left: i64, right: i64) -> i64 {
    left + right
}
