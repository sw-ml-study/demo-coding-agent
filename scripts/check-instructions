#!/bin/sh
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)

# The generated Agentrail briefing block in AGENTS.md must match the embedded
# profile so every session reads the same shared rules.
cd "$repo_dir"
agentrail instructions status
