Add examples/tiny-mlpl-project: lib.mlpl with a module comment and one documented u:add(a, b), and tests/test_add.mlpl in mlplunit style (@test, u:assert_eq, one u:run_registered_tests() call) that passes; do not add it to the root mlplunit.conf discovery, it is the agent's target, and prove it runs with run_script from a test. Harden agents/protocol.mlpl from the live transcripts recorded in step 005: tolerate a line that is only a markdown fence (three backticks, optionally with a language word) directly after WRITE <path> and directly before END, stripping both; add a test for each tolerated habit before implementing it, and add tests proving the strict rejections (missing END, text after END, multiple actions) still hold. Record each new model habit in docs/sw-mlpl-capabilities.md only if it reveals a language issue; otherwise note it in docs/architecture.md under the protocol section. No RUN yet.