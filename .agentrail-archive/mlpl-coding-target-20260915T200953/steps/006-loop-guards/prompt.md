Add two loop guards as pure functions over state, tests first, after a live just mlpl-demo run skipped READ, copied the prompt's example docstring verbatim, invented a test syntax with a wrong include path, and then repeated the identical failing WRITE and RUN until interrupted. (1) Read before write: a WRITE to a path that read_text can read but that has not been READ earlier in this run (state.files) is refused with the observation 'read <path> before writing it' and the loop continues; writing a path that does not exist yet is allowed. (2) Repeated action: if a reply is identical to the previous reply, observe 'REPEATED ACTION: same as the previous step; change your approach' and after the third identical reply stop with reason stuck. Add tests for both in tests/test_loop.mlpl and keep the replay check green. De-anchor prompts/act.md: replace the worked example body text with placeholders that cannot be copied as content (e.g. '<module comment>', '<docstring>;'). Record the run in docs/progression.md as attempt seven with what changed. Then run just mlpl-demo live once with SAVE_FIXTURE=0 and record the outcome honestly.