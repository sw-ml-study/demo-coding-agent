Rust agent-tools extension on the demo-extensions SDK pinned by git rev 2b2ae48: ripgrep-backed search honouring .gitignore with bounded output, allow-listed run (cargo test/check/clippy/fmt, git diff/status) with timeout and captured output, never a shell. Seven cargo tests, clippy pedantic, sw-checklist, and a stock-CLI load_extension probe under just check (skips without cargo). Docs and pin recorded.