Close the loop on the MLPL target. Add a just recipe (just mlpl-demo or LOOP_TASK preset) whose task is: add u:mul(a, b) to examples/tiny-mlpl-project/lib.mlpl, add a test for it in tests/test_add.mlpl, run the tests with RUN mlpl examples/tiny-mlpl-project/tests/test_add.mlpl, and finish when they pass. Prove the flow with a scripted transcript in mlplunit that reads, writes both files, runs, sees PASS, and DONEs, restoring the example files afterwards (the example must stay committed in its original state). Run it live against qwen2.5-coder:7b with LOOP_APPROVE=1, save the transcript as fixtures/transcripts/mlpl-mul-<model>.txt, and record in docs/progression.md how many steps it took and every protocol mistake the model made. If the live model cannot finish within budget, record that honestly and adjust prompts/act.md with a test for each change.