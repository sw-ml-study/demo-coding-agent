Implement agents/loop.mlpl: the bounded observe/decide/act/update loop over a single state record {task, iteration, history, files, budget, done, answer}. Each step: build context from task and history, ask the injected model, parse the action with agents/protocol.mlpl, authorize it against a permissions record via a pure u:authorize returning allow/ask/deny (ask resolves through an injected decision function), execute read and write through read_text and write_atomic, return a 'not available yet' observation for search and run, append ACTION/OBSERVATION to history, and stop on done, deny, or exhausted budget with the stop reason as a value. mlplunit proves a scripted multi-turn transcript that reads examples/tiny-rust-project/src/lib.rs, writes a new file, and finishes with DONE; a denied write stops the loop; and budget exhaustion stops it. Add just loop for an opt-in live run. Update README current status, docs/permissions.md, docs/progression.md, and docs/sagas.md, and close the Saga 1 acceptance in docs/sagas.md. This is the final step of the saga.