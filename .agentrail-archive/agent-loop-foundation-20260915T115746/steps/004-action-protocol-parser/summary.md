Action protocol parser agents/protocol.mlpl with 15 tests written first: READ/SEARCH/WRITE..END/RUN/DONE into tool records, whole-word verbs, CRLF and whitespace tolerance, rejections for empty reply, unknown verb, missing END, multiple actions, trailing text, empty path/text/command, .. components, absolute paths. Ledger F7 (nested if/else dispatch; suggest else-if then match) and F8 (no &&/||, str_trim, list filter/append). 41 tests total.