Add root mlplunit.conf, scripts/run-tests, and a just tests recipe wired into just check. Write native mlplunit probes for the builtins an agent depends on against the adjacent sw-MLPL 0.22.0: read_text/fs_walk/write_text/write_atomic success and err shapes, sandbox escape (../ and symlink) returning err not a hard error, run_script outcome record fields, and the str_find/str_slice/str_split/str_join/str_eq behavior needed for a prefix action protocol. Probe llm_call only as an opt-in script (just llm-probe) that requires explicit OLLAMA_HOST and OLLAMA_MODEL and is not part of just check. Update docs/sw-mlpl-capabilities.md so every row the agent uses moves from documented to measured, and record any bug or justified improvement in the findings queue. Add examples/tiny-rust-project (a Cargo crate with one add function and no tests) as the fixture later agents edit. No agent code yet.