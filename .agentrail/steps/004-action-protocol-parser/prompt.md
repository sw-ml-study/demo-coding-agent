Implement agents/protocol.mlpl: u:parse_action(text) turns exactly one model reply into a record or err. Forms: READ <path>, SEARCH <text>, WRITE <path> newline body newline END, RUN <command>, DONE <summary>. Results are records like {tool: "read", path: ...}, {tool: "write", path: ..., body: ...}, {tool: "run", argv: [...]} with the command split on whitespace, {tool: "done", summary: ...}. Reject with err(...) naming the reason: empty reply, unknown verb, missing END, more than one action, empty path, a path containing .. or starting with /. Write mlplunit tests for every accepted form, every rejection, trailing whitespace, and Windows line endings before implementing. Record in docs/sw-mlpl-capabilities.md how awkward the record-plus-Result encoding was and whether a tagged sum or match form would be justified upstream. No dispatch yet.