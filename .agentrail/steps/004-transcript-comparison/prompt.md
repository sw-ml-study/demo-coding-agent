Record and compare transcripts of the single loop and the planner/builder/reviewer split on the same MLPL task with the same model: steps, wasted steps, wall time, and whether the reviewer ever caught anything. Add scripts/compare-models that runs just mlpl-demo N times per model from OLLAMA_MODELS and tabulates from out/transcripts (finished, steps, parse errors, guard hits, wall) into docs/progression.md. Close Saga 3 with the evidence.