just loop is interactive at a terminal: u:decide_prompt shows the proposed write and approves only y/yes; run-loop picks approve/prompt/refuse and feeds stdin through a FIFO forwarder because all sw-MLPL stdin builtins refuse a TTY (ledger F9, upstream suggestion recorded). Clear refusal message. Gate check for the prompt via pipes; verified under a pty. Demo script restores the example on Ctrl-C and no longer overwrites the committed transcript.