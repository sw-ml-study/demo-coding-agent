Make just loop interactive at a terminal. Add u:decide_prompt to the loop entry (or agents/model.mlpl): it prints the proposed action, tool, path, and for WRITE the body, then reads one line from stdin with the read_stdin builtin and returns 1 only for y or yes. Selection in scripts/run-loop and agents/run_loop.mlpl: LOOP_APPROVE=1 keeps u:decide_yes; LOOP_APPROVE=0 or unset with a TTY on stdin ([ -t 0 ]) uses the prompt; no TTY uses u:decide_no. Replace the denied stop output with a clear message: which action was refused and how to allow it. Measure read_stdin in a mlplunit probe first (returns what on a line, on EOF, on empty input) and record it in the ledger; prove the decide function with a scripted stdin fixture through run_script or a probe script, and keep the loop tests model-free. Update README run instructions, docs/permissions.md ask semantics, and docs/sagas.md.