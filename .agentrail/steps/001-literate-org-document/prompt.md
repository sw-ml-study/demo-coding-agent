Write docs/mlplcode.org, a literate-programming explanation of every MLPL file in the agent, in dependency order: prompts/act.md and prompts/think.md as quoted blocks, then agents/model.mlpl, agents/protocol.mlpl, agents/tools.mlpl, agents/loop.mlpl, agents/replay_loop.mlpl, and the entries run_v0.mlpl, run_loop.mlpl, run_replay.mlpl. For each file: a short section explaining what it owns and why, then each function in its own #+begin_src mlpl block with :tangle <path> and a sentence or two of prose before it explaining the decision it embodies (the protocol choice, the guards, verified completion, why the extension root is a module binding, why strings are joined not added). Include an org header that loads ob-mlpl so C-c C-c on a block evaluates it through sw-MLPL's Emacs support. Prose must be understandable by a reader who knows programming but not MLPL: introduce Result and ?, records, function references and call partials, and include on first use. Add scripts/check-tangle that tangles the org file (emacs --batch -l org --eval with org-babel-tangle, skipping with a notice if emacs is absent) into a scratch directory and diffs each tangled file against the committed source so the document cannot drift; wire it into just check and add just tangle. Keep the committed .mlpl files as the source of truth: the org file must reproduce them byte for byte. Link the document from the README.