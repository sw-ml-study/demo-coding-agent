Add extensions/agent-tools as a Rust cdylib on the demo-extensions ABI and SDK, depending on ../demo-extensions crates by git dependency at a pinned commit (record the pin in docs/architecture.md and docs/cross-repo-handoffs.md). Functions: _agent_tools:search(pattern) over the project root using the ripgrep crates (grep-searcher, grep-regex, ignore) returning matches as path:line:text lines, and _agent_tools:run(argv) that executes only argv prefixes on a fixed allow-list (cargo test, cargo check, cargo clippy, cargo fmt, git diff, git status) in the project root with a timeout, returning status, stdout, and stderr. Public MLPL facade extension.toml plus module.mlpl. Prove with focused cargo tests written first, sw-checklist on the crate, and a stock-CLI load_extension check script wired into just check only when the built library exists. Do not modify ../demo-extensions or ../sw-mlpl.