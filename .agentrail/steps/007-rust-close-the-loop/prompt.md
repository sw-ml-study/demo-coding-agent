Close the loop on the Rust target: the agent adds a unit test for add to examples/tiny-rust-project/src/lib.rs, runs RUN cargo test through the extension, and finishes when it passes. Add an exact old/new PATCH action to the protocol (PATCH <path>, OLD ... END, NEW ... END, or an equivalent documented form) implemented in pure MLPL with read_text, str_find, and write_atomic, tests first, so the model edits regions instead of rewriting files. Prove with a scripted transcript, run live with LOOP_APPROVE=1, save the transcript under fixtures/transcripts/, restore the example crate, and close Saga 2 in docs/sagas.md with its acceptance evidence. This is the final step of the saga.