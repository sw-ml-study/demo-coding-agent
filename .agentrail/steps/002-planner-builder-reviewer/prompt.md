Split the loop into three agents sharing one model: u:planner (read and search only; produces a plan as text), u:builder (read, search, write, patch, run mlpl and cargo), u:reviewer (read, search, run only; judges the diff and tests). Each has its own system prompt under prompts/ and its own permission record per docs/permissions.md. Orchestrate plan -> build -> review with the reviewer able to send the builder back once. Prove with scripted transcripts (planner text feeds the builder task; reviewer approves; reviewer rejects once then approves) and one live run on the MLPL example recorded in docs/progression.md. Compare step counts with the single loop.