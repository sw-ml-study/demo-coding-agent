Implement agents/v0_read_think.mlpl: the smallest coding agent. It reads one file from examples/tiny-rust-project with read_text, builds a TASK/SOURCE prompt, asks a model, and prints the answer. The model is an injected function reference: u:ask_live wraps llm_call with host/model/system arguments passed explicitly, u:ask_scripted replays a fixed reply. mlplunit proves the prompt text, the read failure path (err propagates as a value), and the end-to-end flow with the scripted model. Add prompts/think.md as the system prompt file read with read_text. Add a just v0 recipe that runs live only when OLLAMA_HOST and OLLAMA_MODEL are set. Update README progression status, architecture if the injection shape changed, and the capability ledger findings queue.