Permissions gained git and shell; RUN classified by first word (mlpl pure, cargo under run, git under git, else shell denied, never executed); SEARCH and RUN cargo/git route to the loaded agent-tools extension with not-loaded observations otherwise; extension errors rendered as ERROR; entry loads the library from env, run-loop exports it when built; policy table tested row by row; loop probe with the real extension in the gate. 79 MLPL tests.