Extend the permission record with git and shell fields ({read, search, write, run, git, shell}) and route SEARCH and RUN through the extension when it is loaded: RUN cargo ... and RUN git ... map to _agent_tools:run with git actions authorized under the git field, RUN mlpl ... stays pure MLPL, everything else is shell and denied. u:authorize remains the single authorization point. mlplunit proves the policy table in docs/permissions.md row by row without a model or an extension, and a separate check script proves the extension path when the library exists. Update docs/permissions.md and docs/architecture.md.