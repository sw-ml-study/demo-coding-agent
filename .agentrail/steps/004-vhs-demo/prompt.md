Record the VHS demo of the MLPL close-the-loop run. Add demos/loop.tape for charmbracelet/vhs that runs the mlpl-demo recipe with LOOP_APPROVE=1 at a fixed terminal size and exports assets/demo/loop.gif, loop.webp, and loop.mp4; add a just demo-tape recipe and a script that checks vhs is installed without installing it. Restore the example project after recording. Link the GIF from the README near the loop section with a one-line caption, and note the model and the step count. Keep the recording out of just check.