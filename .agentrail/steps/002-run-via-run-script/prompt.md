Implement RUN for MLPL projects in pure MLPL inside agents/loop.mlpl (or a new agents/tools.mlpl included by it): a RUN action whose argv is exactly ["mlpl", <path>] with a path under the sandbox and no .. maps to run_script(path, {source_dir: <project root>, capture: 1}) and observes a text block with status, value, and the captured test events joined by newlines; any other argv observes 'run: command not allowed: <command>' and does not execute. Keep the allow-list a pure function u:run_allowed(argv) with its own tests. mlplunit proves an allowed run of examples/tiny-mlpl-project/tests/test_add.mlpl reports status ok and a PASS event, a failing test file reports its failure event, a disallowed command is refused, and the loop records the observation. Update docs/permissions.md (RUN allow-list for MLPL) and the ledger if run_script exposes a gap.